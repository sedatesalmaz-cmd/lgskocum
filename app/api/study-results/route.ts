import {NextResponse} from 'next/server';import {env} from 'cloudflare:workers';import {requireMember} from '@/lib/server-auth';
const db=(env as unknown as {DB:D1Database}).DB;
export async function GET(request:Request){try{await requireMember(request,db,['student','guardian','coach'])}catch(error){if(error instanceof Response)return NextResponse.json({error:await error.text()},{status:error.status});return NextResponse.json({error:'Yetkilendirme başarısız.'},{status:500})}const date=new URL(request.url).searchParams.get('date');if(!date)return NextResponse.json({error:'Tarih eksik.'},{status:400});const rows=await db.prepare('SELECT id,assignment_id assignmentId,study_date studyDate,subject_id subjectId,unit,topic,book,total,correct,wrong,blank FROM study_results WHERE study_date=? ORDER BY created_at DESC').bind(date).all();return NextResponse.json({results:rows.results})}
export async function POST(request:Request){try{await requireMember(request,db,['student','guardian','coach'])}catch(error){if(error instanceof Response)return NextResponse.json({error:await error.text()},{status:error.status});return NextResponse.json({error:'Yetkilendirme başarısız.'},{status:500})}const b=await request.json() as {assignmentId:number;studyDate:string;subjectId:string;unit:string;topic:string;book:string;total:number;correct:number;wrong:number;blank:number};if(!b.assignmentId||!b.studyDate||b.total<=0||b.correct+b.wrong+b.blank!==b.total)return NextResponse.json({error:b.total<=0?'Çözülen soru sayısı en az 1 olmalıdır.':'Soru sayıları birbiriyle uyuşmuyor.'},{status:400});const now=new Date().toISOString();await db.prepare('INSERT INTO study_results (assignment_id,study_date,subject_id,unit,topic,book,total,correct,wrong,blank,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(assignment_id) DO UPDATE SET study_date=excluded.study_date,subject_id=excluded.subject_id,unit=excluded.unit,topic=excluded.topic,book=excluded.book,total=excluded.total,correct=excluded.correct,wrong=excluded.wrong,blank=excluded.blank').bind(b.assignmentId,b.studyDate,b.subjectId,b.unit,b.topic,b.book,b.total,b.correct,b.wrong,b.blank,now).run();const row=await db.prepare('SELECT id FROM study_results WHERE assignment_id=?').bind(b.assignmentId).first<{id:number}>();return NextResponse.json({ok:true,id:row?.id,wrongRemaining:b.wrong})}
export async function DELETE(request:Request){try{await requireMember(request,db,['student','guardian','coach'])}catch(error){if(error instanceof Response)return NextResponse.json({error:await error.text()},{status:error.status});return NextResponse.json({error:'Yetkilendirme başarısız.'},{status:500})}const assignmentId=Number(new URL(request.url).searchParams.get('assignmentId'));if(!assignmentId)return NextResponse.json({error:'Görev bilgisi eksik.'},{status:400});await db.prepare('DELETE FROM study_results WHERE assignment_id=?').bind(assignmentId).run();return NextResponse.json({ok:true})}
