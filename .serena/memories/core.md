# Source map
- Root workbook `DENİZ.xlsx` is the legacy planning/topic source and must remain unmodified unless explicitly requested.
- Web app uses root `app/`, `public/`, `components/`, `hooks/`, `lib/`, plus root Vinext/Sites configuration.
- Product: Deniz için LGS study and coaching system; read `mem:product/core` for durable product rules.
- Read `mem:tech_stack` for tooling and `mem:task_completion` before finishing code changes.