# Completion
1. Run `pnpm build`; fix compile and build failures.
2. Run `pnpm lint` if build does not cover it.
3. Confirm the local route returns a successful response before preview handoff.
4. For persistence/auth/remote access, do not claim readiness until implemented and access-control tested.
5. Preserve `DENİZ.xlsx` unless workbook modification was explicitly requested.