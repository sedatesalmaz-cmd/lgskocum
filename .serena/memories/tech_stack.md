# Stack
- TypeScript + React 19, Vinext app routing, Vite, Tailwind CSS 4.
- shadcn/Base UI primitives and Lucide icons installed.
- pnpm lockfile; scripts: dev, build, lint, format.
- Local source uses Sites configuration in `.openai/hosting.json`; current product intent is local-first and secure remote browser access.