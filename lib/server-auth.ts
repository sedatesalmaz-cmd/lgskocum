type Db={prepare:(query:string)=>{bind:(...values:unknown[])=>{first:<T>()=>Promise<T|null>;run:()=>Promise<unknown>};first:<T>()=>Promise<T|null>}};
export async function requireMember(request:Request,db:Db,roles:string[]){const userId=request.headers.get('oai-authenticated-user-id');const email=request.headers.get('oai-authenticated-user-email');if(!userId||!email)throw new Response('Giriş gerekli',{status:401});let member=await db.prepare('SELECT role, subject_id as subjectId FROM members WHERE user_id = ?').bind(userId).first<{role:string;subjectId:string|null}>();if(!member){const count=await db.prepare('SELECT COUNT(*) as count FROM members').first<{count:number}>();if(Number(count?.count??0)!==0)throw new Response('Bu hesap henüz davet edilmemiş',{status:403});await db.prepare('INSERT INTO members (user_id, email, role, created_at) VALUES (?, ?, ?, ?)').bind(userId,email,'guardian',new Date().toISOString()).run();member={role:'guardian',subjectId:null}}if(!roles.includes(member.role))throw new Response('Bu işlem için yetkiniz yok',{status:403});return {userId,email,...member}}
